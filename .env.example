# Supabase 配置（可选）
# 如果设置了这些环境变量，系统将自动连接数据库，无需手动输入
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# 注意：
# 1. 在 Vercel 中，需要在项目设置的 Environment Variables 中添加这些变量
# 2. 本地开发时，复制此文件为 .env.local 并填入实际值
# 3. 不要将包含真实密钥的 .env 文件提交到 Git
